<% if @feed_items.any? %>
  <ol class="microposts">
    <% @feed_items.each do |micropost| %>
      <%= render "microposts/micropost", micropost: micropost %>
    <% end %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>